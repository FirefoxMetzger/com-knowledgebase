# Raw inventory

Snapshot of substantive content under `raw/` and how the wiki indexes it.

## Emails (Inkbox)

| Location | Items | Wiki |
| -------- | ----- | ---- |
| `raw/emails/newsletters/*.md` | **56** Markdown exports (message dates **2026-04-13**–**2026-04-22**; see per-file `fetched_at`) | [[newsletters-catalog]] (full list + `../../raw/...` links); thematic summaries: [[../topics/_index|topics/_index]] |

Other paths under `raw/` may contain only `.gitkeep` placeholders until new material lands.

## Gaps / maintenance

- When new folders appear under `raw/`, extend this table and add or update topic notes.
- Newsletter exports mix **JSON** metadata headers (mailbox CLI) and a few **older YAML-style** headers; behavior is documented in [[newsletters-catalog]].
